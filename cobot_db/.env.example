# Copy this file to `.env` (same directory) and fill in real values.
# `.env` is gitignored — never commit real keys.

# Project URL from Supabase Dashboard -> Project Settings -> API
SUPABASE_URL=https://hjlgdufzoxsgutuyxyze.supabase.co

# Publishable (anon) key. Safe to ship to client devices as long as RLS
# is enabled (it is — see sql/init.sql).
# Format: sb_publishable_xxxxxxxxxxxxxxxx
SUPABASE_KEY=sb_publishable_REPLACE_ME
